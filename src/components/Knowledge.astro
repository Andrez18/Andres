<section class="skills-section" id="habilidades">
    <div class="skills-container">
        <!-- Habilidades principales -->
        <h2 class="skills-title">Habilidades</h2>
        <p class="skills-description">Aquí están las tecnologías y habilidades en las que me especializo:</p>

        <div class="skills-grid">
            <div class="skill-card">
                <div class="skill-icon">
                    <i class="fa-brands fa-html5"></i>
                </div>
                <h3>HTML</h3>
            </div>
            <div class="skill-card">
                <div class="skill-icon">
                    <i class="fa-brands fa-css3-alt"></i>
                </div>
                <h3>CSS</h3>
            </div>
            <div class="skill-card">
                <div class="skill-icon">
                    <i class="fa-brands fa-js"></i>
                </div>
                <h3>JavaScript</h3>
            </div>
            <div class="skill-card">
                <div class="skill-icon">
                    <i class="fa-brands fa-php"></i>
                </div>
                <h3>PHP</h3>
            </div>
        </div>

        <!-- Tecnologías que estás aprendiendo -->
        <h2 class="skills-title">Actualmente aprendiendo</h2>
        <p class="skills-description">Estas son las tecnologías en las que estoy enfocándome actualmente:</p>

        <div class="skills-grid">
            <div class="skill-card">
                <div class="skill-icon">
                    <i class="fa-solid fa-rocket"></i> <!-- Icono genérico para Astro -->
                </div>
                <h3>Astro</h3>
            </div>
            <div class="skill-card">
                <div class="skill-icon">
                    <i class="fa-brands fa-node-js"></i> <!-- Icono de Node.js -->
                </div>
                <h3>NodeJs</h3>
            </div>
        </div>
    </div>
</section>

<style>
    .skills-section {
        margin-top: 20px;
        padding: 60px 20px;
        color: #fff; 
    }

    .skills-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 20px;
    }

    .skills-title {
        font-size: 2.5rem;
        font-weight: 600;
        margin-bottom: 20px;
        text-align: center;
        color: #fff;
    }

    .skills-description {
        font-size: 1.1rem;
        color: rgba(255, 255, 255, 0.8); 
        text-align: center;
        margin-bottom: 50px;
    }

    .skills-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 25px;
        padding: 0 20px;
        margin-bottom: 50px;
    }

    .skill-card {
        background: rgba(255, 255, 255, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.1); 
        border-radius: 10px;
        padding: 25px;
        text-align: center;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        opacity: 0;
        transform: translateY(20px);
        transition: opacity 0.6s ease, transform 0.6s ease;
    }

    .skill-card.visible {
        opacity: 1;
        transform: translateY(0);
    }

    .skill-card:hover {
        transform: translateY(-10px);
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2); 
        border: 1px solid rgba(255, 255, 255, 0.2); 
    }

    .skill-icon {
        margin-bottom: 20px;
    }

    .skill-icon i {
        font-size: 2.5rem;
        color: #5b6ed9; 
        transition: transform 0.3s ease, color 0.3s ease;
    }

    .skill-card:hover .skill-icon i {
        transform: scale(1.2);
        color: #9b59b6;
    }

    .skill-card h3 {
        font-size: 1.5rem;
        margin-bottom: 10px;
        color: #fff;
    }

    .skill-card p {
        font-size: 1rem;
        color: rgba(255, 255, 255, 0.8); 
    }

    @media (max-width: 768px) {
        .skills-grid {
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        }

        .skill-card {
            padding: 20px;
        }

        .skill-icon i {
            font-size: 2rem;
        }

        .skill-card h3 {
            font-size: 1.3rem;
        }

        .skill-card p {
            font-size: 0.9rem;
        }
    }

    @media (max-width: 480px) {
        .skills-grid {
            grid-template-columns: 1fr;
        }

        .skill-card {
            padding: 15px;
        }

        .skill-icon i {
            font-size: 1.8rem;
        }

        .skill-card h3 {
            font-size: 1.2rem;
        }

        .skill-card p {
            font-size: 0.8rem;
        }
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const skillCards = document.querySelectorAll('.skill-card');

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, { threshold: 0.1 });

        skillCards.forEach(card => {
            observer.observe(card);
        });
    });
</script>